import styled from 'styled-components';

const SelectFormContainer = ({ className }) => {
	return <div className={className}>Реализовать тут селект</div>;
};

export const SelectForm = styled(SelectFormContainer)``;
